class Solution:
    def climbStairs(self, n: int) -> int:
        pass
        # recursion to branch off at each point 1 or 2 could be added
        # def stairs_rec(n: int) -> int:
        # base case: n <= 0 return 0
        # base case: n > 0 return 1
        # call stairs_rec with (n - 2) + (n - 1)