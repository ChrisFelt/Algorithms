class Solution:
    def isIsomorphic(self, s: str, t: str) -> bool:
        # construct dictionaries for each word with number of times each letter appears
        # construct lists each word that check the number of times each letter appears with the dicts
        # the lists contain the number of times the letter appears at each index
        # return comparison the lists list1 == list2
        # time complexity will be 2n + 2n + n = O(n)
        pass